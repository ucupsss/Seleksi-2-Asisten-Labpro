{
  "name": "@sso/sync-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "npm run build -w @sso/shared && tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "@sso/shared": "0.0.0",
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "tsx": "^4.23.5",
    "vitest": "^2.1.9"
  }
}
